## 0.1.0

- Initial release
- `@FloatingPaletteApp` and `PaletteAnnotation` for defining palettes
- `Preset` enum for common palette patterns (menu, tooltip, modal, spotlight, persistent)
- Behavior enums for focus, hide-on-click, escape handling
