## 0.1.1

- Rename `lib/builder.dart` to `lib/floating_palette_generator.dart` to match package name convention

## 0.1.0

- Initial release
- Generates `Palettes` class from `@FloatingPaletteApp` annotations
- Type-safe controllers with show/hide/toggle and event listeners
- Preset-based default configuration
